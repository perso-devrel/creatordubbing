{
  "name": "creatordub-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node scripts/postbuild.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.318",
    "typescript": "^6.0.3",
    "vite": "^7.0.0"
  }
}
